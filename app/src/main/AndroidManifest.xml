<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ShoppingListApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ShoppingList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.adding_remote_pad.AddingRemotePadActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.ShoppingList">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="shopping-list.jloc.fr"
                    android:path="/shared"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.about.AboutActivity"
            android:exported="false"
            android:label="@string/activity_about_title"
            android:theme="@style/Theme.ShoppingList" />
    </application>

</manifest>
